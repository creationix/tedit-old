// Press Ctrl+Enter to run this code.
// Then fix the error and run again to get the correct result.
function add(a, b) {
  return a + c;
}

var total = add(4, 6);
print("Added 4 and 6", total);

var it = range(total);
print("Created iterator");

print("Looping through iterator...");
each(it, function (i) {
  print(i + 1);
});
print("Done");